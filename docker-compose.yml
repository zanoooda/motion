services:
  motiond:
    build:
      context: .
      target: release
    image: motion:release
    volumes:
      - ./data:/data
      - ./app:/app
    devices:
      - /dev/video0:/dev/video0
    restart: unless-stopped